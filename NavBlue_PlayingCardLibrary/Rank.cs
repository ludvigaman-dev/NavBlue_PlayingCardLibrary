namespace NavBlue_PlayingCardLibrary
{
    /// <summary>
    /// Rank enum has the possibility to give each rank a custom value in case
    /// of the deck / game values the cards differently than the standard card game.
    /// </summary>
    public enum Rank
    {

        Two = 2,
        Three = 3,
        Four = 4,
        Five = 5,
        Six = 6,
        Seven = 7,
        Eight = 8,
        Nine = 9,
        Ten = 10,
        Jack = 11,
        Queen = 12,
        King = 13,
        Ace = 14,
    }
}
